{
  "swagger": "2.0",
  "info": {
    "title": "Flickr API Broken Schema",
    "version": "1.0.0"
  },
  "basePath": "/services",
  "host": "fake.flickr.com",
  "schemes": [
    "https"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/rest?method=flickr.photos.getInfo": {
      "get": {
        "operationId": "getPhotoByID",
        "description": "Returns a photo",
        "parameters": [
          {
            "name": "api_key",
            "in": "query",
            "required": true,
            "type": "string"
          }
        ]
      }
    }
  }
}
