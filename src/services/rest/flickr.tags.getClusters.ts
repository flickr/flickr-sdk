/**
 * flickr.tags.getClusters
 * Gives you a list of tag clusters for the given tag.
 */
export interface FlickrTagsGetClustersParams {
  /**
   * The tag to fetch clusters for.
   */
  tag: string
}
