/**
 * flickr.people.findByUsername
 * Return a user's NSID, given their username.
 */
export interface FlickrPeopleFindByUsernameParams {
  /**
   * The username of the user to lookup.
   */
  username: string
}
