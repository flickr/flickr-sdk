/**
 * flickr.tags.getListPhoto
 * Get the tag list for a given photo.
 */
export interface FlickrTagsGetListPhotoParams {
  /**
   * The id of the photo to return tags for.
   */
  photo_id: string
}
