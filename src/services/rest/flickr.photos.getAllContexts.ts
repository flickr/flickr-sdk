/**
 * flickr.photos.getAllContexts
 * Returns all visible sets and pools the photo belongs to.
 */
export interface FlickrPhotosGetAllContextsParams {
  /**
   * The photo to return information for.
   */
  photo_id: string
}
