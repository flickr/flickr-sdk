/**
 * flickr.photos.people.getList
 * Get a list of people in a given photo.
 */
export interface FlickrPhotosPeopleGetListParams {
  /**
   * The id of the photo to get a list of people for.
   */
  photo_id: string
}
